import { SupplierCategoryMapping } from './supplier-category-mapping.model';

describe('SupplierCategoryMapping', () => {
  it('should create an instance', () => {
    expect(new SupplierCategoryMapping()).toBeTruthy();
  });
});
