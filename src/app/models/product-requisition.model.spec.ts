import { ProductRequisition } from './product-requisition.model';

describe('ProductRequisition', () => {
  it('should create an instance', () => {
    expect(new ProductRequisition()).toBeTruthy();
  });
});
