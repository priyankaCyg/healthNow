import { ProductCategoryMapping } from './product-category-mapping.model';

describe('ProductCategoryMapping', () => {
  it('should create an instance', () => {
    expect(new ProductCategoryMapping()).toBeTruthy();
  });
});
